# Project setup
## Run the following commands in order in terminal
- `python -m venv .venv`
- `pip install requirements.txt -r`

## Set environment variables
- Create a `.env` file
- Insert the line: OPENAI_API_KEY="your-gemini-api-key"
- Note: use OPENAI_API_KEY even though it is a gemini key